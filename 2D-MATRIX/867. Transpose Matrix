class Solution {
    public int[][] transpose(int[][] matrix) {

        int ans[][]=new int[matrix[0].length][matrix.length];
        for(int i=0; i<matrix.length; i++){
            for(int j=0; j<matrix[i].length; j++){
                ans[j][i]=matrix[i][j];
            }
        }

        return ans;
        
    }
}

/*
📐 Why new int[matrix[0].length][matrix.length]?
matrix.length is the number of rows in the original matrix.

matrix[0].length is the number of columns in the original matrix. */
